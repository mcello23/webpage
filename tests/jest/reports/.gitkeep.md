# Jest Test Reports Directory

This directory contains Jest test reports generated during CI/CD deployment.
The reports are gitignored locally but generated and deployed to GitHub Pages.

- `results-latest.json` - Latest Jest test results in JSON format
