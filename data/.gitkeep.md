# Test Data Directory

This directory contains JavaScript modules with test results data.

- `test-results.js` - Combined Jest and K6 test results (generated by CI/CD)

**Note**: `test-results.js` is generated dynamically during deployment and local development.
Run `./update-test-reports.sh` to generate it locally.
