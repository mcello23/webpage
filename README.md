# Marcelo Costa – QA / SDET Portfolio

[![CI/CD Pipeline](https://github.com/mcello23/webpage/actions/workflows/ci.yml/badge.svg)](https://github.com/mcello23/webpage/actions/workflows/ci.yml)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

Modern, performance‑oriented and accessibility‑aware portfolio highlighting large scale E2E automation, test architecture, and delivery impact (Cypress, Playwright, CI/CD).

## 🌐 Live Demo

Visit the live portfolio: [Link](https://mcello23.github.io/webpage/index.html)

## 🎯 Live Test Dashboard

The webpage includes a **live test results dashboard** that displays:
- ✅ **Jest Test Results**: 1,075 tests across 26 suites, ~85% coverage, pass/fail metrics
- ⚡ **K6 Performance**: HTTP requests, average duration, P95 latency, checks passing
- � **Coverage Metrics**: Statements, branches, functions, lines (with thresholds)
- �🔄 **Auto-refresh**: Updates every 30 seconds with latest test data
- 🎨 **Modal Details**: Scrollable test suites list with custom gradient scrollbar styling

Test reports (Jest/K6 JSON files) and coverage reports are automatically generated and deployed via GitHub Actions CI/CD pipeline.

## ✨ Highlights

### Core

- Fully responsive (mobile → desktop) with progressive enhancement.
- Accessible navigation (ARIA, keyboard friendly modals, semantic structure).
- SEO + rich snippets (JSON‑LD Person schema, meta & Open Graph tags).
- Performance conscious: minimal blocking assets, gradients over large images, no heavy SPA framework.
- Consistent visual system (CSS variables + utility patterns).

### Certificate Gallery (Custom, Vanilla JS)

- Replaced external gallery tooling with a lean modal + grid implementation
- Keyboard support: ArrowLeft / ArrowRight, Escape, Backspace to return to grid
- Lazy thumbnail loading + category badges + optional LinkedIn credential link
- Works from nested pages using dynamic base path resolution (`getBasePath()`)
- 35 certificates with individual validation tests
- Dual export pattern enables unit testing of CertificateModal class

### Test Dashboard (Live Results Display)

- **Real-time test metrics** displayed on homepage in gradient tile
- **Auto-refresh** every 30 seconds with latest test data
- **Jest Results**: Total tests, pass rate, suites, coverage metrics (statements, branches, functions, lines)
- **K6 Performance**: HTTP requests, average duration, P95 latency, checks passing
- **Modal view** with detailed breakdowns:
  - Scrollable test suites list (Jest: max-height 400px, K6: 300px)
  - Custom gradient scrollbar styling matching dashboard theme
  - Expandable sections for metadata, quick stats, and detailed results
- **Instance tracking** with static `destroyAll()` method for proper cleanup
- **Environment guards** prevent auto-initialization during Jest tests
- **Data source**: `test-data.js` generated by CI/CD pipeline
- Dual export pattern (`window.TestDashboard` + CommonJS) for browser + Node testing

### Testing & Quality Signals

- **1,075 Jest tests** across 26 test suites (21 integration + 5 unit) covering:
  - **Unit Tests** for core modules (TestDashboard, CertificateModal, cookie-consent, init)
  - DOM structure & accessibility attributes
  - Link integrity & cross-page consistency
  - Favicon assets & manifest validation
  - Responsive navigation behavior
  - Certificate data validation (35 individual certificate tests)
  - Test dashboard methods (createDashboard, displayError, loadJestResults, loadK6Results, init/destroy)
  - Security (CSP headers, HTTPS, XSS prevention)
  - Performance optimizations (resource loading, lazy loading, minification)
  - Gradient colors & visual consistency
  - Booking CTA & modal wiring
  - Articles & Writing section validation (URLs, text, images, tags, icons)
  - Tech Stack section validation (Shell & Environment, all technologies)
- **~85% code coverage** (statements/lines), **~54% function coverage** (exceeds all thresholds)
- Regex + structural assertions to detect regressions in navigation, Calendly CTA, and modal wiring
- Style presence tests guard against accidental CSS regression (e.g., mobile menu compact design rules)
- **Real-time dashboard** on homepage with auto-refresh (30s intervals) showing Jest/K6 results & coverage metrics
- Comprehensive certificate validation: structure, paths, LinkedIn URLs, categories.

### Side Projects & Frameworks Sections

- Showcases real automation repos (Playwright, Cypress) with contextual tech tags.
- Parallax / gradient sections delineate content zones without JS overhead.

### Accessibility

- High‑contrast actionable elements, focusable interactive regions.
- Explicit aria-labels for icon‑only CTAs (Calendly booking, social links).
- Uses role="list" semantics for custom tag libraries; screen‑reader only headings preserve document outline.

### Calendly CTA (Intentional Mismatch Explained)

The booking links use the event slug `/15min` while retaining the visible & aria wording “Book 15‑min call”. This is intentional per product/branding guidance; tests enforce both the 15min slug and 15‑minute wording so future refactors don’t “fix” it. Update instructions below.

## 📁 Project Structure

```
webpage/
├── index.html              # Main landing page
├── pages/                  # Secondary HTML pages
│   ├── frameworks/
│   │   └── index.html     # Testing frameworks showcase
│   ├── side_proj/
│   │   └── index.html     # Side projects portfolio
│   └── responsive-tester.html # Responsive design tester
│
├── css/                    # Stylesheets
│   ├── certificates.css    # Certificate modal styles
│   ├── cookie-consent.css  # Cookie consent banner styles
│   ├── materialize.css     # Materialize framework
│   ├── materialize.min.css # Minified Materialize
│   ├── navbar.css          # Navigation bar styles
│   ├── prism.css           # Code syntax highlighting
│   ├── style.css           # Main stylesheet
│   └── test-dashboard.css  # Test dashboard styles
│
├── js/                     # JavaScript files
│   ├── certificates.js     # Certificate modal (35 certificates, dual export)
│   ├── cookie-consent.js   # Cookie consent logic (dual export)
│   ├── init.js             # Materialize initialization (dual export)
│   ├── materialize.js      # Materialize framework
│   ├── prism.js            # Code syntax highlighting
│   └── test-dashboard.js   # Test dashboard with auto-refresh (dual export)
│
├── images/                 # Image assets
│   ├── assets/             # Profile & supporting images
│   │   └── DSC_9554.jpg    # Profile photo
│   ├── thumbs/             # Certificate thumbnails (300px standardized)
│   │   └── *.jpg           # 35 optimized thumbnails (8-24KB each)
│   └── *.jpg               # Certificate images (full-size, 35 total)
│
├── favicon/                # Favicon files
│   ├── favicon-16x16.png
│   ├── favicon-32x32.png
│   ├── apple-touch-icon.png
│   └── site.webmanifest
│
├── data/                   # Data files
│   └── test-data.js        # Generated test results (Jest + K6 + coverage)
│
├── utils/                  # Utility scripts
│   └── certificates/
│       ├── certificate-helper.html    # Certificate modal dev tool
│       ├── create-thumbnails.sh       # Initial thumbnail generation
│       ├── regenerate-thumbnails.sh   # Thumbnail standardization
│       ├── download-certificates.js   # LinkedIn certificate downloader
│       └── view-certificates.html     # Certificate viewer
│
├── tests/                  # Test suites (1,075 tests across 26 suites)
│   ├── jest/               # Jest tests (26 suites)
│   │   ├── unit/           # Unit tests (5 suites)
│   │   │   ├── certificates.unit.test.js
│   │   │   ├── cookie-consent.unit.test.js
│   │   │   ├── init.unit.test.js
│   │   │   ├── test-dashboard.unit.test.js
│   │   │   └── test-dashboard.methods.unit.test.js
│   │   ├── [21 integration test files]
│   │   ├── run-tests.sh
│   │   └── reports/
│   │
│   ├── puppeteer/          # Browser tests
│   │   ├── test-puppeteer-realistic.js
│   │   ├── test-puppeteer.js
│   │   ├── run-tests.sh
│   │   └── reports/
│   │
│   └── k6/                 # Performance tests
│       ├── test-k6.js
│       ├── run-tests.sh
│       └── reports/
│
├── .github/
│   └── workflows/
│       └── ci.yml          # CI/CD pipeline (test + deploy + coverage)
│
├── babel.config.js         # Babel configuration
├── eslint.config.js        # ESLint configuration
├── jest.config.js          # Jest configuration (v8 coverage, thresholds)
├── package.json            # Dependencies & scripts
└── README.md               # This file
```

## 🛠️ Tech Stack

### Frontend / UI

- HTML5 semantic layout
- CSS3 (Flexbox, Grid, custom gradients, utility classes)
- Vanilla JavaScript (no SPA framework; dual exports for browser + Node/Jest compatibility)
- Materialize CSS (layout helpers + parallax)
- Font Awesome + Google Material Icons

### Testing & Quality

- **Jest** (v30.2.0) - Unit & integration testing with JSDOM
- **@swc/jest** (v0.2.39) - Fast ES2022 → CommonJS transforms
- **Puppeteer** (v24.29.1) - Realistic browser testing
- **K6** - Performance & load testing
- **ESLint** (v9.38.0) with html plugin
- **Stylelint** (v16.25.0) with standard config
- **Prettier** (v3.6.2) - Code formatting
- **Cheerio** (v1.1.2) - HTML parsing in tests
- **Sharp** (v0.34.4) - Image processing capability

### CI/CD & Deployment

- **GitHub Actions** - Automated testing & deployment pipeline
  - Runs Jest tests with coverage generation
  - Executes K6 performance tests
  - Runs Puppeteer browser tests
  - Deploys test results & coverage reports to GitHub Pages
  - Uploads artifacts (jest-reports, k6-reports, coverage-report, puppeteer-reports)
- **GitHub Pages** - Static site hosting
- **Dependabot** - Automated weekly dependency updates

### Module Architecture

All core JavaScript modules (`certificates.js`, `test-dashboard.js`, `init.js`, `cookie-consent.js`) use **dual export pattern**:
- **Browser globals** (`window.CertificateModal`, `window.TestDashboard`, etc.) for HTML script tags
- **CommonJS exports** (`module.exports`) for Jest/Node testing
- **Environment guards** (`NODE_ENV === 'test'`) prevent auto-initialization during testing
- **Resource cleanup** (destroy methods, interval clearing) for proper test teardown

### Additional Libraries

- Prism.js - Syntax highlighting for framework/code samples
- Chart.js (v4.4.0) - Test dashboard visualizations
- jQuery (v3.6.0) - Materialize dependency
- ImageMagick - Thumbnail optimization & image processing

## � Design & UX

- Mobile‑first layout with progressive scaling
- Distinct content bands (parallax containers) for scannability
- Gradient theming with consistent brand variables (`--brand-1`, `--brand-2`)
- Icon + text pairing for clarity; icon‑only CTAs get ARIA labeling
- Certificate modal splits grid vs. detail view for reduced cognitive load
- **Test Dashboard Modal**: Scrollable test suites list (max-height 400px for Jest, 300px for K6) with custom gradient scrollbar styling

## 📊 Performance Optimizations

- Local (unminified) CSS left readable for development; minification possible for production
- Limited external blocking requests (fonts + icons + Materialize CDN)
- Gradients over large hero bitmaps keep transfer size low
- **Optimized thumbnail images**: All 35 certificate thumbnails standardized to 300px width for consistent quality
- ImageMagick-based thumbnail generation with quality 85 and unsharp filter for optimal clarity
- Non-blocking CSS loading for faster rendering
- Font loading improved with `font-display: swap`
- LCP image prioritized with `fetchpriority`
- Preconnect for CDN resources
- Static cache headers via `_headers` for better repeat-visit performance
- Profile image uses optimized WebP with JPG fallback

## 🧪 Comprehensive Test Suite

This project includes **1,075 automated tests** across **26 test suites**, demonstrating professional QA practices and modern testing methodologies.

### Test Coverage
- **26 test suites** (21 integration + 5 unit)
- **1,075 tests** passing
- **~85% code coverage** (statements/lines)
- **~54% function coverage** (exceeds 35% threshold)
- **Multiple test types**: unit, integration, E2E, performance, security, accessibility
- **Real-time dashboard** on homepage with live test results & coverage metrics
- **CI/CD integrated** via GitHub Actions

### Testing Stack

#### Jest (26 test suites, 1,075 tests)

**Unit Tests** (5 suites in `tests/jest/unit/`):
- `certificates.unit.test.js` - CertificateModal class, getBasePath() utility, escapeHtml() function
- `cookie-consent.unit.test.js` - Cookie consent logic & localStorage interactions
- `init.unit.test.js` - Materialize initialization & jQuery setup
- `test-dashboard.unit.test.js` - TestDashboard module exports & basic functionality
- `test-dashboard.methods.unit.test.js` - TestDashboard methods (createDashboard, displayError, loadJestResults, loadK6Results, loadDataScript with success/error paths, init/destroy/destroyAll)

**Integration Tests** (21 suites in `tests/jest/`):
- `index.test.js` - Homepage tests (300+ assertions covering structure, links, certificates, tech stack)
- `frameworks.test.js` - Frameworks page validation
- `side_proj.test.js` - Side projects page tests
- `responsive-tester.test.js` - Responsive design tester functionality
- `certificates.test.js` - Certificate modal integration tests (35 individual certificate validations)
- `test-dashboard.test.js` - Dashboard data loading, display, and refresh functionality
- `navbar-improvements.test.js` - Navigation bar enhancements and structure
- `navbar-responsive-icon.test.js` - Mobile navigation behavior
- `cross-page.test.js` - Cross-page consistency (navbar, footer, meta tags, styling)
- `security-improvements.test.js` - CSP headers, HTTPS enforcement, XSS prevention
- `performance-optimizations.test.js` - Resource loading, lazy loading, minification
- `unit-performance-optimizations.test.js` - Performance utilities and cache headers
- `gradient-colors.test.js` - Visual styling consistency
- `js-certificates-unit.test.js` - certificates.js structure and logic tests
- `js-init-unit.test.js` - init.js structure tests
- `booking.test.js` - Booking system (Calendly integration)
- `booking-responsive.test.js` - Mobile booking flow
- `contact-form.test.js` - Form validation and attributes
- `cookie-consent.test.js` - Cookie banner integration
- `favicon.test.js` - Favicon & manifest validation
- `all-links.test.js` - Link integrity across all pages

#### Puppeteer (Realistic Browser Testing)
- Full-page screenshots
- Element visibility validation
- Interactive behavior testing
- Visual regression prevention

#### K6 (Performance Benchmarking)
- Load testing (50 VUs, 30s duration)
- HTTP response time monitoring
- Status code validation
- Performance metrics (p95, p99)

### Test Organization
```
tests/
├── jest/                    # Jest unit & integration tests
│   ├── unit/                # Unit tests (5 suites)
│   │   ├── certificates.unit.test.js
│   │   ├── cookie-consent.unit.test.js
│   │   ├── init.unit.test.js
│   │   ├── test-dashboard.unit.test.js
│   │   └── test-dashboard.methods.unit.test.js
│   │
│   ├── [21 integration test files]
│   ├── run-tests.sh         # Jest test runner script
│   └── reports/             # Test results & coverage reports
│       ├── results-latest.json
│       └── coverage-latest/
│
├── puppeteer/               # Realistic browser testing
│   ├── test-puppeteer-realistic.js
│   ├── test-puppeteer.js
│   ├── run-tests.sh
│   └── reports/
│
└── k6/                      # Performance testing
    ├── test-k6.js
    ├── run-tests.sh
    └── reports/
```

### Code Quality & Coverage Thresholds

**Jest Configuration** (`jest.config.js`):
- Coverage provider: v8 (fastest instrumentation)
- Thresholds enforced:
  - Branches: 40%
  - Functions: 35%
  - Lines: 50%
  - Statements: 50%
- Transform: @swc/jest (ES2022 → CommonJS for fast compilation)
- Environment: jsdom (DOM testing) and node (unit tests)

**Current Coverage** (~85% statements/lines, ~54% functions - all thresholds exceeded)

### Quality Tooling

- **ESLint**: JavaScript/HTML linting with html plugin
- **Stylelint**: CSS linting with standard config
- **Prettier**: Code formatting (JS, HTML, CSS, JSON)
- **Unified Quality Commands**:
  - `yarn quality` - Run all lint & format checks
  - `yarn quality:fix` - Auto-fix all linting/formatting issues
- Profile image now uses optimized WebP with JPG fallback.
- Static cache headers via `_headers` for better repeat-visit performance.
- Non-blocking CSS loading for faster rendering.
- Font loading improved with `font-display: swap`.
- LCP image prioritized with `fetchpriority`.
- Preconnect for CDN resources.
- Dependabot enabled for weekly dependency updates.

### Delivery

- Yarn scripts (no bundler required)
- GitHub Pages hosting
- Git version control workflow

## 🚀 Quick Start

### Prerequisites

- Node.js (v22.20.0 or higher)
- Yarn (v4.0.0 or higher) - Project uses Yarn 4.11.0
- Modern web browser
- ImageMagick (optional, for thumbnail generation)

### Installation

1. **Clone the repository**

   ```bash
   git clone https://github.com/mcello23/webpage.git
   cd webpage
   ```

2. **Install dependencies**

   ```bash
   yarn install
   ```

3. **Run tests**

   ```bash
   yarn test
   ```

4. **Run quality checks**

   ```bash
   yarn quality
   ```

5. **Open in browser**
   ```bash
   # Simply open index.html in your browser
   # Or use a local server:
   npx http-server . -p 8080
   ```

## 🧪 Testing

### Run All Tests

```bash
yarn test                    # All tests
yarn test:all               # All tests with coverage
yarn test:unit              # Unit tests only with coverage
yarn test:integration       # Integration tests only
```

### Run Specific Test Frameworks

```bash
yarn test:jest              # Jest tests via shell script
yarn test:k6                # K6 performance tests
yarn test:puppeteer         # Puppeteer browser tests
yarn test:reports           # Generate all test reports
```

### Run Specific Test Suite

```bash
yarn test --testPathPattern=index          # Homepage tests
yarn test --testPathPattern=frameworks     # Frameworks page tests
yarn test --testPathPattern=side_proj      # Side projects tests
yarn test --testPathPattern=unit           # All unit tests
yarn test --testPathPattern=certificates   # Certificate tests
yarn test --testPathPattern=test-dashboard # Dashboard tests
```

### View Coverage Reports

```bash
yarn test:all               # Generates coverage in tests/jest/reports/coverage-latest/
# Open tests/jest/reports/coverage-latest/lcov-report/index.html in browser
```

### Test Coverage Focus

**Unit Tests** (focused module testing):
- CertificateModal class methods and utility functions
- Cookie consent logic and localStorage interactions
- Materialize/jQuery initialization
- TestDashboard module exports and methods (createDashboard, displayError, data loading, init/destroy lifecycle)

**Integration Tests** (comprehensive end-to-end validation):
- Navigation & cross‑page structural consistency
- Favicon & manifest integrity
- Certificate gallery markup hooks & data validation
- All 35 certificates individually tested (structure, paths, URLs, categories)
- Calendly CTA semantics + security attributes
- Responsive navigation (mobile compact menu rules)
- Styling contract tests (presence of gradients, transitions)
- Link integrity across all pages
- Contact form structure and attributes
- Security improvements (CSP headers, HTTPS, XSS prevention)
- Performance optimizations (resource loading, lazy loading, minification, cache headers)
- Test dashboard data loading and display functionality

## 📝 Development

### Project Scripts

```bash
# Testing
yarn test                   # Run all tests
yarn test:unit              # Run unit tests only with coverage
yarn test:integration       # Run integration tests only
yarn test:all               # Run all tests with coverage
yarn test:jest              # Run Jest tests using shell script
yarn test:k6                # Run K6 performance tests
yarn test:puppeteer         # Run Puppeteer browser tests
yarn test:reports           # Generate all test reports (Jest + K6)

# Code Quality
yarn lint                   # Lint JavaScript & HTML
yarn lint:fix               # Auto-fix JavaScript & HTML issues
yarn lint:css               # Lint CSS files
yarn lint:css:fix           # Auto-fix CSS issues
yarn format                 # Format all files (JS, HTML, CSS, JSON)
yarn format:check           # Check formatting without changes

# Quality Commands (Unified)
yarn quality                # Run all linting & format checks
yarn quality:fix            # Auto-fix all linting & formatting issues
```

### Adding Certificates

1. Place full-size image in `images/` (use compressed JPG where possible).
2. The thumbnail will be auto-generated using the standardization script (see below).
3. Append an object to `certificates` array in `js/certificates.js` (increment id):
   ```js
   {
      id: 34,
      title: 'New Certificate Name',
      image: 'images/new-cert.jpg',
      thumb: 'images/thumbs/new-cert.jpg',
      linkedinUrl: null, // or full credential URL
      category: 'Automation'
   }
   ```
4. Run the thumbnail regeneration script to ensure consistent quality:
   ```bash
   cd utils/certificates
   bash regenerate-thumbnails.sh
   ```
5. Tests automatically validate all certificate data (structure, paths, URLs, categories).

### Calendly Configuration

Booking link appears in header social CTA + footer CTA:

- URL (event slug): `https://calendly.com/marceloadsc/15min`
- Visible text: `Book 15-min call`
- aria-label: `Book a 15-minute call`

If you change the event slug:

1. Update all occurrences in: `index.html`, `pages/frameworks.html`, `pages/side_proj.html`.
2. Adjust tests in `__tests__/booking.test.js` (regex patterns) if slug differs.
3. Preserve wording unless brand copy direction changes (tests will fail if mismatched).

## 🎨 Design & UX

- Mobile‑first layout with progressive scaling.
- Distinct content bands (parallax containers) for scannability.
- Gradient theming with consistent brand variables (`--brand-1`, `--brand-2`).
- Icon + text pairing for clarity; icon‑only CTAs get ARIA labeling.
- Certificate modal splits grid vs. detail view for reduced cognitive load.

## 📊 Performance Considerations

- Local (unminified) CSS left readable; could be minified if desired.
- Limited external blocking requests (fonts + icons + Materialize CDN).
- Gradients over large hero bitmaps keep transfer size low.
- **Optimized thumbnail images**: All 35 certificate thumbnails standardized to 300px width for consistent quality and performance.
- ImageMagick-based thumbnail generation with quality 85 and unsharp filter for optimal clarity.
- Opportunity: inline critical CSS + defer non-critical styles (not yet required for current scale).

## 🖼️ Image Optimization

### Thumbnail Standardization

All certificate thumbnails are maintained at a consistent 300px width for optimal display quality. To regenerate thumbnails after adding new certificates:

```bash
cd utils/certificates
bash regenerate-thumbnails.sh
```

**What the script does:**

- Processes all certificate images from `images/*.jpg`
- Generates 300px width thumbnails maintaining aspect ratio
- Applies quality 85 compression for size/quality balance
- Adds unsharp filter (0x0.5) for post-resize clarity
- Strips metadata to reduce file size
- Outputs detailed statistics on completion

**Requirements:**

- ImageMagick installed (`sudo apt-get install imagemagick` on Ubuntu/Debian)

**Results:**

- Consistent thumbnail dimensions (300x223, 300x225, or 300x232 pixels)
- File sizes: 8KB - 24KB per thumbnail
- No pixelation when displayed in certificate cards
- Professional, consistent visual quality across all certificates

## 🔧 Troubleshooting

### Certificate Modal Not Opening

1. Open browser DevTools (F12)
2. Check Console for errors
3. Verify `window.certificateModal` exists:
   ```javascript
   console.log(window.certificateModal);
   ```
4. Try opening manually:
   ```javascript
   window.certificateModal.open();
   ```

### Images Not Loading

- Check file paths are correct (all assets now in `images/` subdirectories)
- Verify images exist in `/images/`, `/images/assets/`, and `/images/thumbs/`
- Check browser console for 404 errors
- Ensure thumbnail paths use `images/thumbs/` prefix

### Tests Failing

```bash
# Clear cache and reinstall
rm -rf node_modules yarn.lock
yarn install
yarn test
```

## 📱 Browser Support

Modern evergreen browsers (Chrome, Firefox, Edge, Safari, iOS/Android). No IE support.

## 📄 License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.

## 👤 Author

**Marcelo Costa** – Senior QA Engineer | SDET

- LinkedIn: https://www.linkedin.com/in/marceloc/
- GitHub: https://github.com/mcello23

## 🙏 Acknowledgments

- **Materialize CSS** - Responsive framework
- **Font Awesome** - Icon library
- **Prism.js** - Syntax highlighting
- **Jest** - Testing framework

## 📈 Changelog

### November 2025

- **Test suite expansion**: Grew from 714 to **1,075 tests** across **26 suites** (21 integration + 5 unit)
  - Added unit test subdirectory (`tests/jest/unit/`) for focused module testing
  - Created `test-dashboard.methods.unit.test.js` with comprehensive method coverage
  - Enhanced `certificates.unit.test.js` with `getBasePath()` tests for nested pages
  - Achieved **~85% statement/line coverage** and **~54% function coverage** (exceeding all thresholds)
- **Certificate expansion**: Added 2 new certificates (Introduction to SQL, Agentic AI for Developers)
  - Total certificates increased from 33 to **35**
  - All new certificates validated with individual tests
- **Test dashboard enhancements**:
  - Instance tracking system with static `destroyAll()` method
  - Environment guards prevent auto-initialization in Jest tests
  - Proper interval cleanup and resource management
  - Production coverage display fix (added coverage-report artifact download in CI)
- **Modal UX improvements**:
  - Scrollable test suites list (max-height 400px Jest, 300px K6)
  - Custom gradient scrollbar styling matching dashboard theme
- **Quality tooling consolidation**:
  - Added `quality` command for unified lint/format checks
  - Added `quality:fix` command for automated fixes
- **Module architecture improvements**:
  - Dual export pattern (browser globals + CommonJS) for all core modules
  - Environment guards (`NODE_ENV === 'test'`) in certificates.js, test-dashboard.js, init.js, cookie-consent.js
  - Proper resource cleanup with destroy() methods

### October 2025

- **Project restructuring**: Consolidated all images into `images/` directory with subdirectories:
  - `images/assets/` for profile and supporting images
  - `images/thumbs/` for certificate thumbnails
  - Root `images/` for full-size certificate images
- **Test expansion**: Grew test suite from 425 to **714 tests** across 14 suites
  - Added `certificates.test.js` with 50+ tests validating all 35 certificates
  - Individual validation tests for each certificate (structure, paths, URLs, categories)
  - Added `gradient-colors.test.js`, `all-links.test.js`, `contact-form.test.js`
  - Enhanced cross-page and responsive testing
- **Image quality improvements**:
  - Created thumbnail standardization script (`regenerate-thumbnails.sh`)
  - Regenerated all 33 thumbnails to consistent 300px width
  - Fixed pixelation issues from inconsistent thumbnail sizes
  - Standardized file sizes (8KB - 24KB) with quality 85 compression
  - Applied unsharp filter for optimal post-resize clarity
- **Path updates**: Updated all file references across HTML, CSS, JS, and test files

### 2025 Q3-Q4

- Custom certificate modal (replaces external gallery dependency)
- Navigation redesign with compact mobile menu & CTA tests
- Accessibility refinements (ARIA roles, labels, list semantics, sr-only headings)
- Calendly integration with booking CTA tests
- Performance optimizations (non-blocking CSS, font-display, fetchpriority, preconnect)
- Security improvements (CSP headers testing, HTTPS enforcement, XSS prevention)

### Earlier

- Initial responsive structure & content sections
- Base automation project listings & framework highlights
- GitHub Pages deployment setup

## 🗺️ Roadmap (Future Ideas)

- Theme toggle (light/dark).
- Inline Calendly popup widget (progressive enhancement).
- Blog / article feed integration.
- i18n (EN + PT/ES) for broader reach.
- ~~Automated image optimization pipeline (Sharp script) + critical CSS extraction.~~ ✅ **Completed: ImageMagick-based thumbnail standardization**

---

**Built with care by Marcelo Costa | © 2025**
